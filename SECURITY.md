# 보안 정책

HAE.NA는 회의 오디오와 전사 원문을 다루고 사용자의 OpenAI API 키를 보관합니다. 그래서 버그 하나가
바로 개인정보 문제가 될 수 있습니다.

## ⚠️ 이슈나 로그에 절대 올리지 마세요

버그를 신고할 때 다음은 **어떤 경우에도 첨부하지 마세요.**

- **API 키** — 일부만, 앞 몇 글자만도 안 됩니다
- **회의 오디오 파일**
- **전사 원문** — 실제 회의 내용
- **`projects.json`, `profile.json`, `agent-jobs.json`** — 회의·참석자·업무·알림 시각이 들어 있습니다
- OpenAI API 응답 원문
- 참석자 실명이 보이는 스크린샷

대신 이렇게 보내주세요.

- 무엇을 했고 무엇이 일어났는지 (재현 절차)
- 오류 메시지 **문구**
- 앱 버전, macOS 버전
- 필요하다면 **직접 만든 가짜 데이터**로 재현한 결과

키를 실수로 공개된 곳에 올렸다면, 신고보다 먼저 **OpenAI 대시보드에서 그 키를 즉시 폐기**하세요.

## 취약점 신고

일반 버그는 GitHub Issues로 올려주세요.

**보안 취약점은 GitHub Security Advisory로 비공개 신고해주세요.**

저장소의 **Security 탭 → Report a vulnerability**에서 비공개 초안을 만들 수 있습니다.
관리자만 볼 수 있고, 수정이 끝난 뒤 함께 공개할 수 있습니다.

**하지 말아야 할 것:**

- 🚫 **공개 이슈·PR·토론에 취약점 세부사항을 올리지 마세요.** 수정 전에 악용될 수 있습니다.
- 🚫 **개인 이메일로 보내지 마세요.** 이메일은 추적되지 않고, 전달 과정에서 남습니다.
- 🚫 **어느 경로로든 실제 API 키나 회의 데이터를 첨부하지 마세요.** Security Advisory도
  예외가 아닙니다.

신고에 포함해주시면 좋은 것: 영향 범위, 재현 절차, 앱·macOS 버전, 그리고 필요하다면 **직접 만든
가짜 데이터**로 재현한 결과.

> ℹ️ 저장소를 공개한 뒤 **Settings → Code security → Private vulnerability reporting**을 켜야
> 이 창구가 실제로 열립니다. 아직 켜지지 않았다면 저장소 소유자에게 알려주세요.

## 지원 범위

Developer Preview이며, **보안 수정은 `main`의 최신 상태에만** 반영합니다. 과거 Preview 빌드에 대한
백포트는 하지 않습니다.

## API 키 취급

- 키는 macOS Keychain에만 저장합니다 (`com.haena.HAENA` / `openai-api-key`).
- 접근성은 `kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly`로, 백업을 통해 다른 기기로 넘어가지
  않습니다.
- 저장된 키는 화면에 다시 표시되지 않습니다. 마스킹된 형태로도 보여주지 않습니다.
- 오류·상태·로그에 키가 포함되지 않도록 하고, 이를 자동 테스트로 확인합니다.
- 앱은 **개발자 공용 키를 포함하지 않습니다.** 사용자가 자신의 키를 넣는 BYOK 방식입니다.

### 우선순위

키는 한 곳(`OpenAICredentialResolver`)에서만 결정되며 순서는 다음과 같습니다.

1. **환경변수 `OPENAI_API_KEY`**
2. **Keychain**
3. 없음

환경변수가 Keychain보다 우선합니다. 개발·테스트 시 셸에서 주는 값이 명시적·한시적 override이기
때문입니다. **키를 저장했는데 다른 키가 쓰이는 것 같다면 환경변수가 설정돼 있는지 확인하세요.**
앱의 `AI 설정` 화면은 어느 경로가 쓰이고 있는지 표시합니다(값은 표시하지 않습니다).

## unsigned 빌드의 한계

이 저장소가 배포하는 `.app`은 **Developer ID 서명도 공증도 되어 있지 않습니다.** 그 결과:

- macOS가 개발자 신원을 확인해주지 않습니다. **파일이 배포 후 변조되지 않았음을 Apple이
  보증하지 않습니다.**
- 설치 시 Gatekeeper 경고를 사용자가 직접 넘겨야 합니다.
- 따라서 **출처와 SHA-256 체크섬 확인이 사용자 몫**입니다. 공식 릴리스 페이지의 값과 대조하세요.

```bash
shasum -a 256 HAE.NA-<버전>-<빌드>-unsigned.app.zip
```

- Gatekeeper를 전역으로 끄는 방법(`spctl --master-disable`)은 권장하지 않습니다. 앱 하나 때문에
  시스템 전체 보호를 낮출 이유가 없습니다.
- 신뢰할 수 없는 곳에서 받은 빌드는 실행하지 마세요. **소스에서 직접 빌드하는 것이 가장 안전한
  경로입니다.**

## 알려진 보안 관련 한계

- 로컬 데이터는 평문 JSON입니다(파일 권한은 소유자 전용). 디스크 암호화가 필요하면 FileVault를
  사용하세요.
- 삭제는 secure erase가 아닙니다.
- App Sandbox를 적용하지 않았습니다. 기존 저장 경로가 이동해 사용자 데이터가 고아가 되는 문제를
  피하기 위한 의도된 선택이며, 별도 마이그레이션 과제로 남아 있습니다.
- 여러 앱 인스턴스가 동시에 실행되면 같은 저장 파일을 두고 경합할 수 있습니다.
